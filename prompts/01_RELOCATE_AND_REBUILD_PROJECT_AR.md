# Prompt 01 — نقل المشروع إلى مجلد منظم جديد

أنشئ مجلدًا جديدًا باسم `piscine-forge-rebuilt/`.

المطلوب:

1. لا تعدّل الأرشيفات القديمة.
2. استخرجها في `.tmp/legacy_extract/`.
3. اقرأ بنيتها.
4. أنشئ المشروع الجديد بالهيكل التالي:
   - `subjects/`
   - `corrections/`
   - `pools/`
   - `docs/`
   - `schemas/`
   - `piscine_forge/`
   - `workspace/`
5. انسخ فقط الأفكار والمنطق الضروري، لا تنسخ spaghetti code كما هو.
6. اكتب تقريرًا في `docs/LEGACY_TOOL_ANALYSIS.md`.
